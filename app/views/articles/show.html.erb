<section id="main">
	<article class="post">
		<header>
			<p><time pubdate datetime="<%= @post.created_at %>"><%= @post.created_at.to_date.to_formatted_s(:long) %></time></p>
			<h2><%= @post.title %></h2>
		</header>
		<%= simple_format(@post.body) %>
	</article>
</section>
